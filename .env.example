# Copy to .env and fill in. Everything except DATABASE_URL is optional —
# the app runs on seeded mock data with no keys.

# ---- Database (SQLite by default) ----
DATABASE_URL="file:./dev.db"

# ---- AI advisor (optional) ----
# Get a key at https://console.anthropic.com  → Settings → API Keys
# Without it, the advisor uses the built-in rule-based brain.
ANTHROPIC_API_KEY=
# Optional model override (default: claude-sonnet-4-6)
ADVISOR_MODEL=

# ---- Plaid bank linking (optional) ----
# Get sandbox keys at https://dashboard.plaid.com  → Developers → Keys
PLAID_CLIENT_ID=
PLAID_SECRET=
# sandbox | development | production
PLAID_ENV=sandbox

# ---- App login ----
# Leave APP_PASSWORD blank locally to keep the app open during development.
# Set it in production to require this password before anyone can see your data.
APP_PASSWORD=
# A long random string used to sign the login cookie (any 32+ random chars).
APP_SECRET=
